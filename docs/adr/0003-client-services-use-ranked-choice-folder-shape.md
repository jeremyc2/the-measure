# Thin commands calling explicit service boundaries

Even without a backend, browser capabilities live in PascalCase Effect service folders under `src/frontend/services/`. This preserves the architecture habit of thin Foldkit commands calling explicit service boundaries, while keeping this repo static and client-only.
